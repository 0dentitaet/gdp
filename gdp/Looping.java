public class Looping {
    long N = 106;//Long.parseLong(args[0]);
    long m
    public static void main(String[] args) {
        if (Long.parseLong[].length >=0 ) 
            {System.out.println("");
            System.exit(-1);
        }
            long N1=N; // Originalwert von N speichern
            long N2=N;
            long N3=N;
            
    static long reverse () {
        while (N != 0) {
            long lastDigit = N % 10; // get last digit of N
            R = R * 10 + lastDigit; // Add the last digit to R and digit shift the previous value of R
            N = (int) N / 10; // remove the last digit from N
    
        }
    }
}
    

